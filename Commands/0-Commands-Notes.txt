Commands that create things:
/room, /exit, /area, /item, /weapon, /armor, /mob, /shop, etc.

Each of these commands should have the following features:
create (make a new object of that type)
set (set properties of the object)
delete (remove the object)
list (list all objects of that type)
help (provide help information about the command)

